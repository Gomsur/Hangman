# Your code goes here.
# You can delete these comments, but do not change the name of this file
# Write your code to expect a terminal of 80 characters wide and 24 rows high

import random
from animals import word_list as animals
from country import word_list as country
from food import word_list as food

class gamesetting:
    """
    Choose game category
    """
    def __init__(self, lives, words):
        self.lives = lives
        self.hidden = ""
        if word == animals
        self.animals()
        else if == "country":
        self.country()
        else:
        self.food()


def get_animal